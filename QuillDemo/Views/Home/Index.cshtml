@{
    ViewData["Title"] = "Home Page";
}

@foreach (var item in Model)
{

    <div>
        @Html.Raw(item.Content)
    </div>
}

@if (Model.Count == 0)
{
    <div class="text-center">
        <h1 class="display-4">Welcome</h1>
        <p>No Data added for this page</p>
    </div>
}
